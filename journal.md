#Journal

Interface:
- парсинг строки параметров и их проверка на стороне C++
- обработка ошибок в канале interface <-> app
- single source of truth описания возможных конфигураций интерконнекта.

01.04.2026:
Починил mesh_router и mesh, протестировал все, что есть на данный момент с interconnect.


!!!!! Port не реализует задержку!!!!
TODO: по-хорошему пару портов стоит объединять в Channel.

Что дальше?
Далее нужна обертка над MeshInterconnect, которая позволит кормить данными сеть и принимать пакеты, собирая информацию о запусках.

19/04:

маски портов перенес в Router.
сделал обвязку для MeshInterconnect: см. src/interconnect/network:
1) IP Core - вычислитель, отправляет/принимает пакеты через один роутер.
2) SimulationController: максимально простой интерфейс, в формате
```cpp
    sim.setup_mesh(2, 2);  // 4 узла
    sim.generate_uniform_trace(50,     // 50 пакетов
                                0.2,   // injection rate
                                100);  // max ticks
    sim.run(100);
```
3) packet_trace - генерация трафика (4 вида)

Пока почему-то hops не считается, будем разбираться...
- Пофиксил, просто нигде не увиличивался счетчик хопов

В тестах увидел hop_average > latency_average - вспомнил о проблеме с портами.
Далее пофиксил отсутствие задержки в порту: теперь interconnect вызывает сначала on_clock у всех routers_, после - у всех портов в all_ports_, теперь нельзя прочитать из порта на том же такте, где произошла запись.

Дальнейшие шаги:
- Подвязка реализованных интерфейсов в JNI
- Создание базы данных с результатами симуляций

21/04

Разделяю все Java скрипты на несколько папок по ответственности, улучшено окно GUI. Нужно описать в Readme структуру проекта и сборку.

обновили GUI, теперь описать структуру проекта в README

Что мне не очень нравится:
- нет SingleSource of Truth описания реализованных типов интерконнектов и маршрутизации для Java и C++ сторон
- симуляция сейчас продолжает идти даже после того, как все пакеты были отправлены. Необходим режим на заданном числе тактов, когда пакеты постоянно пересылаются вплоть до последнего такта симуляции

Что дальше делаем:
- из обязательного: подтянуть базу данных, в которой будут храниться результаты запусков симуляции, нужно что-то вроде логов с результатами и просто база с перфами разных конфигураций интерконнектов (допустим, для каждой конфигурации самый последний успешный запуск).
- Хочется heatmap нагрузки узлов и каналов смотреть
- Дальше уже можно другие топологии добавлять и типы маршрутизации.
